A badge-access containment rollup shipped during the Northgate incident is producing an unreliable responder queue, and you need to put it right. Restore the reconciler at /app/workflow/export_access.py so it matches what the access-review board actually decided, and add an audit tool at /app/access_audit.py with two subcommands: diagnose, which writes a stateless defect report covering all six known deployment defects, and repair, which rebuilds the rollup, runs it, and leaves summary.json, zone_matrix.json, contained.jsonl, diagnosis.json and repair_audit.json in the output directory, defaulting to /app/output. The board's dossier at /app/incident/access_review_dossier.md is the sole authority for how every value is derived, from badge-class normalization and duplicate handling through session stitching, control-window scope and precedence, the occupancy ledger, admission, priority and ordering. Most of it is routine noise, and its February proposals were reversed while several March notes were revised again in May, so trace each rule to its latest dated decision. The output contract at /app/docs/report_spec.json fixes the exact key sets, field types, digest payloads and checksum serialization, and one wrong key or type fails an artifact outright. Leave the read-only frozen snapshot untouched. Finish by running repair and leaving the result in the output directory.
